# ============================================================
# GeoCanvas — Root environment variables (CI/CD only)
# ============================================================
# Used by GitHub Actions / CI for deploying Convex functions.
# Do NOT use for local dev — run `npx convex dev` instead.
# ============================================================

# Convex production deployment (CI deploy)
# Get: https://dashboard.convex.dev  → project settings → Deploy Key
CONVEX_DEPLOYMENT=""
CONVEX_DEPLOY_KEY=""
