{
  "name": "@geocanvas/crdt",
  "version": "0.0.0",
  "private": true,
  "description": "Purpose-built OR-set + HLC CRDT for canvas strokes (ADR-002 pending benchmark)",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "benchmark": "vitest bench",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "rimraf": "^6.0.1"
  }
}
